import Listener from "../../components/Broadcasting/Listener";

const LiveListener = () => {
    return (
        <div>
            <Listener />
        </div>
    );
};

export default LiveListener;
